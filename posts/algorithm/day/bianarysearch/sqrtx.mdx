---
id: 69
title: 69. x 的平方根 
date: '2022-06-28'
level: easy
hot: 4
author:
  name: NateWang
  picture: 'https://media.wangbaoqi.tech/assets/authors/logo.png'
postShow: true
tags:
  - 二分法
category: Algorithm
subCategory: everyDay
---


**LeetCode**[x 的平方根](https://leetcode.cn/problems/sqrtx/)

> 给你一个非负整数 **x** ，计算并返回 x 的 算术平方根 。由于返回类型是整数，结果只保留 整数部分 ，小数部分将被舍去 。
> 注意：不允许使用任何内置指数函数和算符，例如 pow(x, 0.5) 或者 `x ** 0.5` 。


**示例 1：**

```
输入：x = 4
输出：2
示例 2：
```

**示例 2：**

```
输入：x = 8
输出：2
解释：8 的算术平方根是 2.82842..., 由于返回类型是整数，小数部分将被舍去。
```




### 二分法

采用**二分法**，需要注意的有以下几点：

1. 确定查找的区间是闭区间还是开区间，这里是**闭区间`[l,r]`**，也就决定了**m**的取值。

2. 确定**l**和**r**是否需要相遇，也就是查询结束条件；

3. 确定需要返回的值（考虑到是整数）

```ts
const mySqrt = (x: number): number => {
  if(x < 2) return x;

  let l = 1, r = x;

  while(l <= r) {
    let m = l + ((r - l) >> 1);
    if (m ** 2 === x) {
      return m
    } else if(m ** 2 < x) {
      l = m + 1;
    } else {
      r = m - 1;
    }
  }
  return r
}
```

### 执行代码

export const js = `
const mySqrt = (x) => {
  if(x < 2) return x;
  let l = 1, r = x;
  while(l <= r) {
    let m = l + ((r - l) >> 1);
    if (m ** 2 === x) {
      return m
    } else if(m ** 2 < x) {
      l = m + 1;
    } else {
      r = m - 1;
    }
  }
  return r
}`
export const options = {
  mainEntry: 'mySqrt'
}

<PlayGround js={js} mode='algo' options={options}/> 
