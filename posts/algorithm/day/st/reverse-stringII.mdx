---
id: 541
title: 541.反转字符串II
date: '2022-03-03'
level: easy
hot: 4
author:
  name: NateWang
  picture: 'https://media.wangbaoqi.tech/assets/authors/logo.png'
postShow: true
tags:
  - 字符串
category: Algorithm
subCategory: everyDay
---

**LeetCode** [反转字符串II](https://leetcode.cn/problems/reverse-string-ii/)

> 给定一个字符串 `s` 和一个整数 `k`，从字符串开头算起，每计数至 `2k` 个字符，就反转这 `2k` 字符中的前 `k` 个字符。
> - 如果剩余字符少于 `k` 个，则将剩余字符全部反转。
> - 如果剩余字符小于 `2k` 但大于或等于 `k` 个，则反转前 `k` 个字符，其余字符保持原样

**示例 1：**

```markdown
输入：s = "abcdefg", k = 2
输出："bacdfeg"
```

**示例 2：**

```
输入：s = "abcd", k = 2
输出："bacd"
```

**提示：**

- `1 <= s.length <= 104`
- `s` 仅由小写英文组成
- `1 <= k <= 104`


## 模拟法

```js
const reverseStr_d = (s, k) => {
  let a = s.split('');
  let len = a.length;
  let i = 0;
  const reverse = (l, r) => {
    while (l < r) {
      let t = a[l];
      a[l] = a[r];
      a[r] = t;
      l++;
      r--;
    }
  }
  while (i < len) {
    let l = i, r = Math.min(i + k, len) - 1;
    reverse(l, r)
    i += 2*k 
  }
  return a.join('')
}
```

## 执行代码

export const js = `
const reverseStr_d = (s, k) => {
  let a = s.split('');
  let len = a.length;
  let i = 0;
  const reverse = (l, r) => {
    while (l < r) {
      let t = a[l];
      a[l] = a[r];
      a[r] = t;
      l++;
      r--;
    }
  }
  while (i < len) {
    let l = i, r = Math.min(i + k, len) - 1;
    reverse(l, r)
    i += 2*k 
  }
  return a.join('')
}`
export const options = {
  mainEntry: 'reverseStr_d'
}

<PlayGround js={js} mode='algo' options={options}/> 
